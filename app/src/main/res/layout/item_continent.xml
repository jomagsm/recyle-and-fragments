<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="4dp"
    android:paddingHorizontal="8dp"
    android:paddingVertical="8dp"
    android:background="@drawable/card_border">

    <ImageView
        android:id="@+id/image_continent"
        android:layout_width="75dp"
        android:layout_height="75dp"
        app:layout_constraintStart_toStartOf="parent"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/title_continent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="12dp"
        android:text="Example"
        android:textSize="28sp"
        app:layout_constraintStart_toEndOf="@+id/image_continent"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/description_continent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="5"
        android:text="example long text"
        app:layout_constraintStart_toStartOf="@+id/title_continent"
        app:layout_constraintTop_toBottomOf="@+id/title_continent" />
</androidx.constraintlayout.widget.ConstraintLayout>